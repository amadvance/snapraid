SnapRAID INSTALL
================

As root or user type:

    ./configure
    make

and as root to install the programs and the documentation
type:

    make install

then change the example configuration file snapraid.conf to fit
your needs and copy it in /etc/snapraid.conf.

The documentation is in the man page:

    man snapraid
